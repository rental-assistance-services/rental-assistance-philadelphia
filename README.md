# pr-assets

Screenshots referenced from pull-request comments. This branch has no history in common with
`main`, is never merged, and is not published by GitHub Pages (Pages builds from `main`).

- `pr-7/` — before (`main` @ 346c3e0) and after (`landlord-check-popup` @ 713a6de), at 1280×800
  and 390×844. Captured with Playwright against the files served as-is; Photon and the CRM
  endpoint stubbed, analytics blocked.
